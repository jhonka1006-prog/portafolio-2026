# Public variables for Vite (safe for client-side)
VITE_PUBLIC_SITE_URL=https://example.com

# Private variables: do NOT commit real values.
# Configure them directly in Vercel Project Settings > Environment Variables.
